# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Database Configuration (Railway MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=siamdrug_app
DB_PORT=3306
DB_POOL_LIMIT=10

# Session Configuration
SESSION_SECRET=your_very_secure_session_secret_here_change_this_in_production

# Railway Database Environment Variables (Automatically provided by Railway)
# MYSQL_URL=mysql://user:password@host:port/database
# MYSQL_HOST=your-railway-mysql-host
# MYSQL_USER=your-railway-mysql-user
# MYSQL_PASSWORD=your-railway-mysql-password
# MYSQL_DATABASE=your-railway-mysql-database
# MYSQL_PORT=3306

# Instructions:
# 1. Copy this file to .env: cp env.example .env
# 2. Update the values with your actual configuration
# 3. For Railway deployment, use the MySQL service and Railway will automatically
#    provide MYSQL_* environment variables
# 4. Never commit .env file to version control
